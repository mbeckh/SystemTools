#include "systools/Backup.h"

namespace systools::test {

TEST(Backup, Copy) {
	std::vector src{Path(L"T:\\test")};

	//Backup backup;
	//backup.CompareDirectories(src, Path(L"V:\\ref"), Path(L"V:\\out"));
}

}  // namespace systools::test
